server {
  resolver 8.8.8.8;
  listen 10000;

  # # Health endpoint for Render health check
  # location /health {
  #   return 200 "OK";
  #   add_header Content-Type text/plain;
  # }

  # # Main location block (handles OPTIONS + fallback)
  # location / {
  #   # Preflight requests
  #   if ($request_method = OPTIONS) {
  #     add_header Access-Control-Allow-Origin *;
  #     add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
  #     add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
  #     add_header Access-Control-Max-Age 86400;
  #     return 204;
  #   }

  #   # Default fallback for all non-DICOMweb paths
  #   return 404;
  # }


# # Use ^~ to ensure this block takes priority over any other matches
# location ^~ /pacs-proxy/ {
#     # 1. Handle Preflight (OPTIONS) requests immediately
#     if ($request_method = 'OPTIONS') {
#         add_header 'Access-Control-Allow-Origin' '*' always;
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
#         add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept' always;
#         add_header 'Access-Control-Max-Age' 86400;
#         return 204;
#     }

#     # 2. Proxy the actual data requests
#     proxy_pass http://webquiz-orthanc-server.onrender.com/dicom-web/;
    
#     proxy_set_header Host webquiz-orthanc-server.onrender.com;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;

#     # 3. Force CORS headers on all successful and error responses
#     add_header 'Access-Control-Allow-Origin' '*' always;
#     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
#     add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept' always;
# }

location /health {
    return 200 "OK";
    add_header Content-Type text/plain;
}


# Use a prefix match that is NOT a directory match
location /pacs-proxy {
    # 1. Kill Nginx's ability to redirect
    absolute_redirect off;
    port_in_redirect off;
    
    # 2. Extract the path using a named capture or rewrite
    # If the user requests /pacs-proxy/studies
    # $1 becomes /studies
    rewrite ^/pacs-proxy(/.*)$ $1 break;

    # 3. Now proxy the result
    # proxy_pass http://webquiz-orthanc-server.onrender.com/dicom-web$uri;
    
    # Actually, let's use the most reliable variable-based approach
    set $target "webquiz-orthanc-server.onrender.com";
    proxy_pass http://$target/dicom-web$uri;

    proxy_set_header Host $target;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    # CORS
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept' always;

    if ($request_method = 'OPTIONS') {
        return 204;
    }
}

}



# # # >>>>>>>>>>>>>>>>>>>>>>  Matches the path /pacs-proxy/ defined in webQuiz.conf instead of /dicom-web
# # # >>>>>>>>>>>>>>>>>>>>>>    note the trailing / on pacs-proxy and not on dicom-web
# # # >>>>>>>>>>>>>>>>>>>>>>  Used to enforce Nginx to recognize the path and since Render
# # # >>>>>>>>>>>>>>>>>>>>>>      doesn't recognize pacs-proxy, it doesn't intercept it to help out 
# location /pacs-proxy/ {
#     # Strip /pacs-proxy/ and pass to Orthanc's /dicom-web/
#     proxy_pass http://webquiz-orthanc-server.onrender.com/dicom-web/;
    
#     # 2. Correct Host Header
#     proxy_set_header Host webquiz-orthanc-server.onrender.com;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;

#     # 3. CRITICAL: Handle the redirect that Orthanc IS sending.
#     # Orthanc is sending "Location: /studies". 
#     # We must rewrite that to "Location: /dicom-web/studies"
#     proxy_redirect /studies /dicom-web/studies;

#     # 4. Force CORS
#     add_header 'Access-Control-Allow-Origin' '*' always;
#     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
#     add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept' always;

#     # 5. Handle OPTIONS
#     if ($request_method = 'OPTIONS') {
#         add_header 'Access-Control-Allow-Origin' '*';
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE';
#         add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept';
#         add_header 'Access-Control-Max-Age' 86400;
#         return 204;
#     }
# }